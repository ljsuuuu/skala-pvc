server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        
        # SPA 라우팅의 핵심: 파일이나 경로를 못 찾으면 무조건 index.html로 돌려보냄
        try_files $uri $uri/ /index.html;
    }
}